name: AI PR Review (self-review)

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - uses: ./
        with:
          api-key: ${{ secrets.AI_API_KEY }}
